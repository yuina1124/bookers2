 <h2>User info</h2>
      <% if user.image.attached? %>
        <%= image_tag user.image, size:"100×100" %>
      <% else %>
        <%= image_tag 'default-image', size: "100×100" %>
      <% end %>
      <table class="table">
        <tbody>
        <tr></tr>
        <tr>
          <th>name</th>
          <th><%= user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= user.introduction %></th>
        </tr>
        </tbody>
      </table>
      <div class="row">
        <a class="btn btn-outline-secondary btn-block edit_user_9008" href="/users/9008/edit">
          <i class="fas fa-user-cog"></i>
        </a>
      </div>